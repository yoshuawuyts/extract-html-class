{
  "name": "extract-html-class",
  "description": "Extract all classes from html",
  "repository": "yoshuawuyts/extract-html-class",
  "version": "1.0.0",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "start": "node .",
    "test": "standard && npm run deps && node test"
  },
  "dependencies": {},
  "devDependencies": {
    "dependency-check": "^2.9.1",
    "standard": "^10.0.2",
    "tape": "^4.7.0"
  },
  "keywords": [
    "html",
    "classname",
    "extract",
    "regex"
  ],
  "license": "MIT"
}
